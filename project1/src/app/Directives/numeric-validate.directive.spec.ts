import { NumericValidateDirective } from './numeric-validate.directive';

describe('NumericValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericValidateDirective();
    expect(directive).toBeTruthy();
  });
});
